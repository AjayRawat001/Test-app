const routes = (module.exports = require('next-routes')());
/**
 * Whenever you make any changes to this file,
 * you will need to restart the server in order to see the effect
 */
routes.add({ name: 'Secondpage', pattern: '', page: 'secondpage' });
